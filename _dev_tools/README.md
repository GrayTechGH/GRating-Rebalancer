Development-only helpers.

Do not include this folder in released Calibre plugin zips. A template release
payload should contain only runtime plugin modules, `LICENSE`, and runtime
assets such as `images/plugin_icon.png`.
